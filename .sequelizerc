const { resolve } = require('path')

module.exports = {
  'config': resolve('src', 'config', 'database.js'),
  'models-path': resolve('src', 'core'),
  'migrations-path': resolve('src', 'infrastructure', 'database', 'migrations')
}
