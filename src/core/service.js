export default ({
  database,
  Logger
}) => {
  const createPartner = body => {
    return body
  }

  const findPatnerById = event => {

  }

  const findNearestPartner = event => {

  }

  return {
    createPartner,
    findPatnerById,
    findNearestPartner
  }
}
